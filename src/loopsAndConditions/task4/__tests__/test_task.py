from __future__ import annotations

from loopsAndConditions.task4.task import task


def test_function():
    assert task('Dog', 2) == 'Fqi'
    assert task('Zak zak', 3) == 'Cdn cdn'
    assert task('Python is the BEST', 5) == 'Udymts nx ymj GJXY'
